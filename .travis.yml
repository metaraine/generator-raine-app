language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-npm-module' ]; then cd .. && eval "mv $currentfolder generator-npm-module" && cd generator-npm-module; fi

